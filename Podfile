# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'
use_frameworks!
target 'Psiduck' do
  # Pods for Psiduck
  pod 'Alamofire'
  pod 'SwiftyJSON'

end

target 'PsiduckTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Quick'
    pod 'Nimble'
    pod 'SwiftyJSON'
end
